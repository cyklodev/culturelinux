# Docker

## images
### List
    docker images
    docker images --all
    docker images moulti-stream

### Build
    docker build -f DockerFile -t moulti-stream:0.0.9 .

### Remove 
    docker image prune -a -f